import { Database } from '../../types/database';

// Здесь вы можете определить кастомные типы или расширить сгенерированные.
// Например, можно создать типы для отдельных таблиц для удобства.

// TODO: Раскомментируйте эти строки после того, как сгенерируете типы командой
// npx supabase gen types typescript --project-id <your-project-ref> > types/database.ts
// export type User = Database['public']['Tables']['users']['Row'];
// export type Post = Database['public']['Tables']['posts']['Row'];
// export type Contract = Database['public']['Tables']['contracts']['Row'];

// ... и так далее для других таблиц.

// Этот файл поможет вам работать с типизированным клиентом Supabase.
// После генерации `types/database.ts` убедитесь, что пути и названия таблиц верны. 